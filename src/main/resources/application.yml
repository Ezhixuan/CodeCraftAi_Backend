server:
  port: 8911
  servlet:
    context-path: /api

spring:
  application:
    name: CodeCraftAi_backend
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: 1q2w3e4r%
    url: jdbc:mysql://localhost:3307/code_craft_ai_db
  profiles:
    active: @profiles.active@


# springdoc-openapi
springdoc:
  group-configs:
    - group: 'default'
      packages-to-scan: com.ezhixuan.common.common_backend.controller
# knife4j
knife4j:
  enable: true
  setting:
    language: zh_cn

# system
system:
  default-password: # 默认密码
